# Jest test paths known to be unrelated baseline failures for Ralph loop full-suite gate.
# One regex per line. Blank lines and lines starting with # are ignored.
# Utility simulation/statistics suites are intentionally one-off analysis runs,
# not deterministic regression gate tests.
src/lib/utility-tests/
src/data/utility-tests/
